import React, { useState } from "react";
import ExpenseForm from "./ExpenseForm";

function AddNewExpense(props) {
  return <button onClick={expand}>Add New Expense</button>;
}

export default AddNewExpense;
